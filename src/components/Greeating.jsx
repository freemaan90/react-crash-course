export const Greeating = () => {
  const user = {
    name: "Jon",
    surname: "Doe",
  };

  const add = (a, b) => {
    return a + b;
  };

  return <h1>{add(30, 10)}</h1>;
};

export const UserCard = ({ name, amount }) => {
  console.log(props);
  return <div></div>;
};
